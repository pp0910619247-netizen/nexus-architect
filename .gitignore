# Secrets
.env
**/PRIVATE_KEY*
*privatekey*
contracts/.env

# Node / Hardhat
contracts/node_modules/
contracts/artifacts/
contracts/cache/
contracts/types/

# Foundry
contracts/cache/
contracts/out/
contracts/lib/

# OS / Editor
Thumbs.db
.DS_Store
.vscode/

# Model binaries (large GGUF files — never push to GitHub, soft-limit 100MB)
models/
*.gguf

# Android machine-specific (SDK path — per developer)
mobile/NexusApp/local.properties
mobile/NexusApp/.idea/

# Local Netlify folder
.netlify

# Netlify CLI (local tooling only — not part of the site)
node_modules/
dist/
package.json
package-lock.json
